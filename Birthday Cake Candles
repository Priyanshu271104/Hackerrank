#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int a[n],i,max,count;
    for(i=0;i<n;i++)
    scanf("%d",&a[i]);
    max=a[0];
    for(i=1;i<n;i++)
    {
        if(max<=a[i]);
        max=a[i];}
        
    for (i=0; i<n; i++)
    {
        if(a[i]==max)
        count+=1;
    }
    printf("%d",count);
    return 0;
}
