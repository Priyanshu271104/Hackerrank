#include <bits/stdc++.h>
using namespace std;
int main(){
    int i,n,c;
    cin>>n>>c;
    vector<int> a(n);
    for(i=0;i<n;i++){
        cin>>a[i];
    }
    for(i=c;i<n;i++){
        cout<<a[i]<<' ';
    }
    for(i=0;i<c;i++){
        cout<<a[i]<<' ';
    }
    return 0;
}
